const DEFAULT_DEST =  './.import';
const DEFAULT_FILENAME = '{slug}.md';
const DEFAULT_RESPONSE_PATH = null;
const DEFAULT_CONTENT_PATH = 'content';
const FRONT_MATTER_DELIMITER = '---';

module.exports = {
  DEFAULT_DEST,
  DEFAULT_FILENAME,
  DEFAULT_CONTENT_PATH,
  FRONT_MATTER_DELIMITER
};
